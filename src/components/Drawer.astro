<div class="container">
    <slot/>
    <div class="drawer">
        <slot name="right"/>
    </div>
</div>

<style lang="scss">
    .container {
        display: grid;
        grid-template-columns: minmax(0, 2fr) minmax(350px, 1fr);
        gap: .5rem;

        @media (max-width: $md) {
            grid-template-columns: 0 1fr;
            gap: 0;
        }
    }

    .drawer {
        animation: 250ms ease-out 0s 1 drawer-intro;

        @keyframes drawer-intro {
            0% {
                transform: scale(0.975);
            }
            70% {
                transform: scale(1.01);
            }
            100% {
                transform: scale(1);
            }
        }
    }
</style>